# Shopee OS — AIエージェント向けの前提

このリポジトリを触る前に読むこと。人間の新メンバーにも同じことを説明する。

---

## これは何か

日本から **7か国（PH / SG / MY / BR / VN / TH / TW）** へ出すShopeeの越境ECを、
**各国のSeller Centerを開かずに1つのポータルで回す**ための業務システム。個人事業＋外注2名で実運用中。
止まると売上が止まる。実験場ではない。

| ファイル | 中身 |
|---|---|
| `index.html` | ポータル本体。**33,000行超・3.5MB・全体が1つのIIFE**。素のJS/CSS、ビルド無し |
| `shopee-openapi-sync.gs` | Shopee公式Open API v2 のプロキシ（署名が要るので必ずここ経由）＋定期同期 |
| `shopee-*.user.js` | Tampermonkey。公式APIで取れないものだけ担当（通称ブリッジ） |
| その他 `*.gs` | 為替・追跡・通知など単機能 |

配信は GitHub Pages（`main` にpushで自動デプロイ）。データは Supabase を PostgREST で直叩き。

---

## 却下済み。提案しないこと

1. **モジュール分割／React・Vue・Vite・バンドラの導入** — 1ファイル・ビルド無しは**意図的**。
   GitHub Pagesに置いて全PC・スマホから即開けること、編集が1ファイルで完結することが要件。
2. **「Seller Centerでやればいい」** — ポータルに集約するのが目的そのもの。目的の否定になる。
3. **購入・決済の自動化** — 絶対にしない。
4. **外部の利益管理スプレッドシートへの書き戻し** — ポータルとスプシは独立並行運用。連携させない。
5. **「押さないと出ない／開かないと見えない」UI** — 外注が使うので、入力→結果は打鍵連動、
   一次データは最初から表示。畳んでよいのは補足だけ。

---

## 壊れる場所（すべて実際に壊した）

### TDZ — これでポータル全体が死ぬ
全体が1つのIIFEなので、`const`/`let` を**使用箇所より後ろに宣言すると実行時に全機能が停止**する。
`./chk.sh` の構文チェックは通り、ログイン画面は静的HTMLなので普通に表示される。押しても無反応になって初めて気づく。
→ **デプロイしたら必ずブラウザのコンソールを読む。**

### GASの接続枠は1日20,000回
JST16:00リセット。使い切ると**出品・明細の追加や削除・画像が全部止まる**。過去2回枯らしている。
- `UrlFetchApp.fetchAll(reqs)` は **1回の呼び出しで `reqs.length` 回ぶん枠を食う**
- 画像は1枚につき取得1回＋アップロード1回＝**2回**
- 背景同期は15,000回で自動停止（`UF_STOP`）。この安全弁を壊さない
- 残量は GAS の `uf_status` で読める。ポータル上部のピルにも常時出ている

**呼び出しを増やす変更は、削減とセットでなければ入れない。**

### バリエ明細は「途中を消すと後ろが消える」
Shopee公式APIは位置（`tier_index`）が変わる明細を保持できない。過去に3件失った。
→ **詰め直し方式**（下の中身を1つ上へ書き写し、あまった末尾の枠だけ削除／`delete_variation_shift`）。
**この実装を素朴な削除に戻さない。** 空き枠は詰めない・並び替えない。

### VNの上限は2つある。別物
- ① **出品価格 1,000,000 VND未満**（100万以上は出せない）
- ② **1注文の購入限度額 5,000,000 VND**（決済側）

②の値を①に入れると出せない価格が通ってしまう。1件でも超過が混ざると `add_model` が丸ごと失敗する。

### 集計から静かに落ちる「空」
`orders.total` が344件空で **¥2,528,426が売上から落ちていた**。空は画面に出ないので気づけない。
数字が合わないときは、**まず空の件数を数える**。

### 日付と為替
- `orders.order_date` は**各国のローカル日付**。日本時間の集計は `order_ts` → `jstDate` を優先
- 円換算は **注文日(JST)基準の日次レート** `app_kv.fx_daily` × 国別実効スプレッド。
  確定日基準にすると過去月の数字が動く。レートのハードコード禁止

### 入金の確定タイミング
**倉庫スキャンの数日後**に確定する。注文完了時ではない。配送中でもスキャン済みなら確定済み。
SLS補償や関税で**数か月後に再変動する**。

### 仕入元の og:image は商品写真とは限らない
メルカリShopsは全商品で同じ1枚を返す。**同じ写真が2件以上の明細に付いたら貼らない。**
間違った写真を貼るより貼らない方がずっとマシ。

### 表示ラベルと実データの矛盾
`tab !== 300 → 発送済` のような決め打ち分岐は、else側に別状態が混ざる。過去に6件是正した。
発送期限は**日単位で数える表と時刻基準のバッジを同じ画面に混ぜない**。

---

## 変更したら必ず通す締め

```bash
./chk.sh                                    # index.html と全 .gs の構文チェック
bash ~/.claude/skills/portal-release/check.sh   # 締め作業の点検（Claude Code環境のみ）
```

1. **改修履歴**（`index.html` 内の `renderChangelog`）に今日の分 ＝ *何が変わったか*
2. **英語版**（`index.html` の `I18N` 辞書）＝ 日本語UIを足したら必ず英語も足す。放置すると虫食いになる
3. **デプロイ後にコンソールを読む**（上記TDZのため。省略不可）
4. GASを変えたなら **Webアプリの新バージョンを再デプロイ**（保存だけでは反映されない）

GASは**全部入りの最新ソースで丸ごと貼り替える**。断片ファイルを作らない・ファイルを増やさない。
貼る前に関数名と開始行を突き合わせ、**GASにしか無い関数を消していないか**確認する。

---

## 指摘の作法

- **推測で直さない。実際にコードとデータを見てから直す。**
- 「たぶん危ない」は要らない。**壊れる筋道が言えるものだけ**出す
- 行番号は `index.html:12345` の形で示す
- 数字を出すときは**2通りで検算する**（再描画時間を2回誤って報告した前科がある）
